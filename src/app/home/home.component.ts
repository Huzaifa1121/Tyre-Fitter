import { Component } from '@angular/core';

// Example: home.component.ts
@Component({
  selector: 'app-home', // Make sure this matches the selector in your parent component's template
  templateUrl: './home.component.html', // Check that the file path is correct
})
export class HomeComponent { }

